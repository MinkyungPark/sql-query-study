ANIMAL_INS 테이블에서,
이름이 두번 이상 쓰인 이름과 해당 이름의 횟수를 조회,
이름이 없는 동물은 제외하고 결과는 이름순

SELECT NAME, COUNT(NAME) FROM ANIMAL_INS
WHERE NAME IS NOT NULL
GROUP BY NAME HAVING COUNT(NAME) >= 2
ORDER BY NAME;

하지만 GROUP함수는 자동으로 NULL을 제외함.

SELECT NAME, COUNT(NAME) FROM ANIMAL_INS
GROUP BY NAME HAVING COUNT(NAME) >= 2
ORDER BY NAME;