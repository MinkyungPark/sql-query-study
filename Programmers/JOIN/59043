보호소에 들어온 날이
입양간 날보다 나중인 동물의 아이디와 이름 조회

먼저 외래키인 ID로 조인 하고 조건 적용

SELECT I.ANIMAL_ID AS ANIMAL_ID, I.NAME as NAME
FROM ANIMAL_INS AS I, ANIMAL_OUTS AS O
WHERE O.ANIMAL_ID = I.ANIMAL_ID AND I.DATETIME > O.DATETIME
ORDER BY I.DATETIME